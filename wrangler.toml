name = "signature-capture-worker"
# Keep the Worker on the newest runtime supported by the pinned Wrangler version.
# Recheck behavior when this date changes or Wrangler is upgraded.
compatibility_date = "2026-06-02"
preview_urls = true

[build]
command = "npm run build"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
